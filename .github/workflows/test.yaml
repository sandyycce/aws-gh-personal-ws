name: Test

on:
  push:
    branches:
      - master
    
  workflow_dispatch:

jobs:
  log_message:
    runs-on: ubuntu-latest
    steps:
      - name: Log a message
        run: echo "Hello, this is a test message!"